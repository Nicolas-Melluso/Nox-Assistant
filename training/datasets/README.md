Dataset folders for Fase 0.

- `raw/` : place raw intent CSVs here (e.g. intents_p99_template.csv).
- `processed/` : output of processing scripts.

Populate `raw/intents_p99_template.csv` with at least 99 examples per intent for p99 target.
